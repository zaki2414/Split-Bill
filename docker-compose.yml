services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: splitbill
      POSTGRES_PASSWORD: splitbill
      POSTGRES_DB: splitbill
    ports:
      - "5432:5432"
    volumes:
      - splitbill_pgdata:/var/lib/postgresql/data

volumes:
  splitbill_pgdata:
